#!/usr/bin/env -S fontforge -quiet
# -*- mode: python; coding: utf-8 -*-
import fontforge, argparse, re

def main():
    global args
    parser = argparse.ArgumentParser()
    parser.add_argument("filename")
    parser.add_argument("--glyphs")
    args = parser.parse_args()

    font = fontforge.open(args.filename)
    print("font.activeLayer: %s" % repr(font.activeLayer))
    print("font.ascent: %s" % repr(font.ascent))
    print("font.bitmapSizes: %s" % repr(font.bitmapSizes))
    print("font.capHeight: %s" % repr(font.capHeight))
    print("font.changed: %s" % repr(font.changed))
    print("font.cidcopyright: %s" % repr(font.cidcopyright))
    print("font.cidfamilyname: %s" % repr(font.cidfamilyname))
    print("font.cidfontname: %s" % repr(font.cidfontname))
    print("font.cidfullname: %s" % repr(font.cidfullname))
    print("font.cidordering: %s" % repr(font.cidordering))
    print("font.cidregistry: %s" % repr(font.cidregistry))
    print("font.cidsubfont: %s" % repr(font.cidsubfont))
    print("font.cidsubfontcnt: %s" % repr(font.cidsubfontcnt))
    print("font.cidsubfontnames: %s" % repr(font.cidsubfontnames))
    print("font.cidsupplement: %s" % repr(font.cidsupplement))
    print("font.cidversion: %s" % repr(font.cidversion))
    print("font.cidweight: %s" % repr(font.cidweight))
    print("font.comment: %s" % repr(font.comment))
    print("font.copyright: %s" % repr(font.copyright))
    # print("font.cvt: %s" % repr(font.cvt))
    print("font.default_base_filename: %s" % repr(font.default_base_filename))
    print("font.descent: %s" % repr(font.descent))
    print("font.design_size: %s" % repr(font.design_size))
    print("font.em: %s" % repr(font.em))
    print("font.encoding: %s" % repr(font.encoding))
    print("font.familyname: %s" % repr(font.familyname))
    print("font.fondname: %s" % repr(font.fondname))
    print("font.fontlog: %s" % repr(font.fontlog))
    print("font.fontname: %s" % repr(font.fontname))
    print("font.fullname: %s" % repr(font.fullname))
    print("<font.gasp>")
    for entry in font.gasp:
        print("    %s" % repr(entry))
    print("</font.gasp>")
    print("font.gasp_version: %s" % repr(font.gasp_version))
    print("<font.gpos_lookups>")
    for name in font.gpos_lookups:
        print("    %s" % repr(name))
    print("</font.gpos_lookups>")
    print("<font.gsub_lookups>")
    for name in font.gsub_lookups:
        print("    %s" % repr(name))
    print("</font.gsub_lookups>")
    # print("font.guide: %s" % repr(font.guide))
    print("font.hasvmetrics: %s" % repr(font.hasvmetrics))
    print("font.head_optimized_for_cleartype: %s" % repr(font.head_optimized_for_cleartype))
    print("font.hhea_ascent: %s" % repr(font.hhea_ascent))
    print("font.hhea_ascent_add: %s" % repr(font.hhea_ascent_add))
    print("font.hhea_descent: %s" % repr(font.hhea_descent))
    print("font.hhea_descent_add: %s" % repr(font.hhea_descent_add))
    print("font.hhea_linegap: %s" % repr(font.hhea_linegap))
    print("font.horizontalBaseline: %s" % repr(font.horizontalBaseline))
    print("font.is_cid: %s" % repr(font.is_cid))
    print("font.is_quadratic: %s" % repr(font.is_quadratic))
    print("font.isnew: %s" % repr(font.isnew))
    print("font.italicangle: %s" % repr(font.italicangle))
    print("font.macstyle: %s" % repr(font.macstyle))
    print("font.layer_cnt: %s" % repr(font.layer_cnt))
    # print("font.layers: %s" % repr(font.layers))
    print("font.loadState: %s" % repr(font.loadState))
    print("font.maxp_FDEFs: %s" % repr(font.maxp_FDEFs))
    print("font.maxp_IDEFs: %s" % repr(font.maxp_IDEFs))
    print("font.maxp_maxStackDepth: %s" % repr(font.maxp_maxStackDepth))
    print("font.maxp_storageCnt: %s" % repr(font.maxp_storageCnt))
    print("font.maxp_twilightPtCnt: %s" % repr(font.maxp_twilightPtCnt))
    print("font.maxp_zones: %s" % repr(font.maxp_zones))
    print("font.multilayer: %s" % repr(font.multilayer))
    print("font.onlybitmaps: %s" % repr(font.onlybitmaps))
    print("font.os2_capheight: %s" % repr(font.os2_capheight))
    print("font.os2_codepages: %s" % repr(font.os2_codepages))
    print("font.os2_family_class: %s" % repr(font.os2_family_class))
    print("font.os2_fstype: %s" % repr(font.os2_fstype))
    print("font.os2_panose: %s" % repr(font.os2_panose))
    print("font.os2_strikeypos: %s" % repr(font.os2_strikeypos))
    print("font.os2_strikeysize: %s" % repr(font.os2_strikeysize))
    print("font.os2_stylemap: %s" % repr(font.os2_stylemap))
    print("font.os2_subxoff: %s" % repr(font.os2_subxoff))
    print("font.os2_subxsize: %s" % repr(font.os2_subxsize))
    print("font.os2_subyoff: %s" % repr(font.os2_subyoff))
    print("font.os2_subysize: %s" % repr(font.os2_subysize))
    print("font.os2_supxoff: %s" % repr(font.os2_supxoff))
    print("font.os2_supxsize: %s" % repr(font.os2_supxsize))
    print("font.os2_supyoff: %s" % repr(font.os2_supyoff))
    print("font.os2_supysize: %s" % repr(font.os2_supysize))
    print("font.os2_typoascent: %s" % repr(font.os2_typoascent))
    print("font.os2_typoascent_add: %s" % repr(font.os2_typoascent_add))
    print("font.os2_typodescent: %s" % repr(font.os2_typodescent))
    print("font.os2_typodescent_add: %s" % repr(font.os2_typodescent_add))
    print("font.os2_typolinegap: %s" % repr(font.os2_typolinegap))
    print("font.os2_unicoderanges: %s" % repr(font.os2_unicoderanges))
    print("font.os2_use_typo_metrics: %s" % repr(font.os2_use_typo_metrics))
    print("font.os2_vendor: %s" % repr(font.os2_vendor))
    print("font.os2_version: %s" % repr(font.os2_version))
    print("font.os2_weight: %s" % repr(font.os2_weight))
    print("font.os2_weight_width_slope_only: %s" % repr(font.os2_weight_width_slope_only))
    print("font.os2_width: %s" % repr(font.os2_width))
    print("font.os2_winascent: %s" % repr(font.os2_winascent))
    print("font.os2_winascent_add: %s" % repr(font.os2_winascent_add))
    print("font.os2_windescent: %s" % repr(font.os2_windescent))
    print("font.os2_windescent_add: %s" % repr(font.os2_windescent_add))
    print("font.os2_xheight: %s" % repr(font.os2_xheight))
    # print("font.path: %s" % repr(font.path))
    print("font.persistent: %s" % repr(font.persistent))
    # print("font.math: %s" % repr(font.math))
    # print("font.private: %s" % repr(font.private))
    print("font.privateState: %s" % repr(font.privateState))
    # print("font.selection: %s" % repr(font.selection))
    # print("font.sfd_path: %s" % repr(font.sfd_path))

    print("<font.sfnt_names>")
    for entry in font.sfnt_names:
        print("    %s" % repr(entry))
    print("</font.sfnt_names>")

    print("font.sfntRevision: %s" % repr(font.sfntRevision))
    print("font.size_feature: %s" % repr(font.size_feature))
    print("font.strokedfont: %s" % repr(font.strokedfont))
    print("font.strokewidth: %s" % repr(font.strokewidth))
    # print("font.style_set_names: %s" % repr(font.style_set_names))
    print("font.temporary: %s" % repr(font.temporary))
    print("font.texparameters: %s" % repr(font.texparameters))
    print("font.uniqueid: %s" % repr(font.uniqueid))
    print("font.upos: %s" % repr(font.upos))
    print("font.userdata: %s" % repr(font.userdata))
    print("font.uwidth: %s" % repr(font.uwidth))
    print("font.version: %s" % repr(font.version))
    print("font.verticalBaseline: %s" % repr(font.verticalBaseline))
    print("font.vertical_origin: %s" % repr(font.vertical_origin))
    print("font.vhea_linegap: %s" % repr(font.vhea_linegap))
    print("font.weight: %s" % repr(font.weight))
    print("font.woffMajor: %s" % repr(font.woffMajor))
    print("font.woffMinor: %s" % repr(font.woffMinor))
    print("font.woffMetadata: %s" % repr(font.woffMetadata))
    print("font.xHeight: %s" % repr(font.xHeight))
    if args.glyphs:
        print("<font.glyphs>")
        for glyph in font.glyphs():
            print("    <glyph %s %s altuni=%s encoding=%s>" % (repr(glyph.glyphname), glyph.unicode, glyph.altuni, glyph.encoding))
            print("        glyph.activeLayer: %s" % repr(glyph.activeLayer))
            print("        glyph.altuni: %s" % repr(glyph.altuni))
            print("        glyph.anchorPoints: %s" % repr(glyph.anchorPoints))
            print("        glyph.anchorPointsWithSel: %s" % repr(glyph.anchorPointsWithSel))
            # print("        glyph.background: %s" % repr(glyph.background))
            print("        glyph.changed: %s" % repr(glyph.changed))
            print("        glyph.color: %s" % repr(glyph.color))
            print("        glyph.comment: %s" % repr(glyph.comment))
            print("        glyph.dhints: %s" % repr(glyph.dhints))
            print("        glyph.encoding: %s" % repr(glyph.encoding))
            # print("        glyph.font: %s" % repr(glyph.font))
            # print("        glyph.foreground: %s" % repr(glyph.foreground))
            print("        glyph.glyphclass: %s" % repr(glyph.glyphclass))
            print("        glyph.glyphname: %s" % repr(glyph.glyphname))
            print("        glyph.hhints: %s" % repr(glyph.hhints))
            print("        glyph.horizontalComponents: %s" % repr(glyph.horizontalComponents))
            print("        glyph.horizontalComponentItalicCorrection: %s" % repr(glyph.horizontalComponentItalicCorrection))
            print("        glyph.horizontalVariants: %s" % repr(glyph.horizontalVariants))
            print("        glyph.isExtendedShape: %s" % repr(glyph.isExtendedShape))
            print("        glyph.italicCorrection: %s" % repr(glyph.italicCorrection))
            print("        glyph.layer_cnt: %s" % repr(glyph.layer_cnt))
            # print("        glyph.layers: %s" % repr(glyph.layers))
            # print("        glyph.layerrefs: %s" % repr(glyph.layerrefs))
            print("        glyph.lcarets: %s" % repr(glyph.lcarets))
            print("        glyph.left_side_bearing: %s" % repr(glyph.left_side_bearing))
            print("        glyph.manualHints: %s" % repr(glyph.manualHints))
            print("        glyph.mathKern.bottomLeft: %s" % repr(glyph.mathKern.bottomLeft))
            print("        glyph.mathKern.bottomRight: %s" % repr(glyph.mathKern.bottomRight))
            print("        glyph.mathKern.topLeft: %s" % repr(glyph.mathKern.topLeft))
            print("        glyph.mathKern.topRight: %s" % repr(glyph.mathKern.topRight))
            print("        glyph.originalgid: %s" % repr(glyph.originalgid))
            print("        glyph.persistent: %s" % repr(glyph.persistent))
            print("        glyph.references: %s" % repr(glyph.references))
            print("        glyph.right_side_bearing: %s" % repr(glyph.right_side_bearing))
            print("        glyph.script: %s" % repr(glyph.script))
            print("        glyph.temporary: %s" % repr(glyph.temporary))
            print("        glyph.texheight: %s" % repr(glyph.texheight))
            print("        glyph.texdepth: %s" % repr(glyph.texdepth))
            print("        glyph.topaccent: %s" % repr(glyph.topaccent))
            print("        glyph.ttinstrs: %s" % repr(glyph.ttinstrs))
            print("        glyph.unicode: %s" % repr(glyph.unicode))
            print("        glyph.unlinkRmOvrlpSave: %s" % repr(glyph.unlinkRmOvrlpSave))
            print("        glyph.user_decomp: %s" % repr(glyph.user_decomp))
            print("        glyph.userdata: %s" % repr(glyph.userdata))
            print("        glyph.vhints: %s" % repr(glyph.vhints))
            print("        glyph.validation_state: %s" % repr(glyph.validation_state))
            print("        glyph.verticalComponents: %s" % repr(glyph.verticalComponents))
            print("        glyph.verticalComponentItalicCorrection: %s" % repr(glyph.verticalComponentItalicCorrection))
            print("        glyph.verticalVariants: %s" % repr(glyph.verticalVariants))
            print("        glyph.width: %s" % repr(glyph.width))
            print("        glyph.vwidth: %s" % repr(glyph.vwidth))
            print("    </glyph>")
        print("</font.glyphs>")

    print("glyph widths: %s" % repr(list(set([glyph.width for glyph in font.glyphs()]))))

    font.close()

main()
